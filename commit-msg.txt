feat(video): add audio unlock overlay on first interaction

- Replace muted autoplay with an interactive audio-unlock prompt
- Full-screen overlay with "Bật âm thanh" button lets browser grant audio permission
- Once tapped, audio stays unlocked for all subsequent videos in the session
- Video auto-plays (muted) immediately; user taps once to unmute
